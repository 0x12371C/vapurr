# ORG FLASH — Relic · 2026-09-04 · do not drift

Read this before you touch a file. If your change is not on a lane below, **do not do it**.

## Lanes (only these)

| Lane | Owner | In | Out |
|------|-------|----|-----|
| **v1 zip** | House | Install vapurr.exe, WebView2 browse, Scan, shield, fomo, chrome | Servo, Rain, pack wars, kill-vapurr |
| **v1.2 money** | Pilot | 46630 `$PUSD` / `$VAPURR` book. KetPay `wallet-send` **testnet only**. HouseLp / PusdLoop **empty CAs** until Relic signs | New DeFi tabs, mainnet 4663 spend, USDG as product dollar |
| **Ketflix** | KFX | Posters + trailers already in `frontend/ketflix/` | More grok procs. Do **not** grow the zip. Last pack **50.6 MB** because mp4s rust-embed |
| **KetPay / id** | PayId | Honest NeedKyc. `vapurr://pay` is 402. `vapurr://id` opens thesecretlab.app/kyc. No fake Proven | Branding pay as 404. Live issuer in-tree |
| **install_id** | Bind | Minted on Install. Profile file. Uninstall keeps it. Portable does not mint | Re-opening setup.rs |
| **PARK** | — | Charts / Psy / Tube | Any landing. `ketcharts.html` was touched ~09:34 — freeze |

KEEP windows: **House** (this session, sole packer), **Pilot** (desk, no pack).

ACTIVE grok (already running — do not spawn more): KFX 21016, PayId 9228, Bind 28128.

## Canon (locked)

- **404** = load-fail page (`404.png`). Never payments.
- **KetPay** = HTTP **402** / x402 / `$PUSD`.
- Home chain **4663**. Econ / spend bootstrap **46630**.
- Earn payout = KYC + `install_id`. Chrome does not fake Proven.
- Cat. Lime `#c0f800`. Sora. No second accent.
- VEIL (VAI/COL) is not vapurr `$PUSD` unless Relic links them.

## Stop

- New `grok.exe` / PowerShell worker windows.
- `pack.ps1` unless Relic or House is packing (House only).
- After-v1: Servo, Rain settle, mainnet `$PUSD`.
- New `vapurr://` ids. `euler` / `house` / `lp` already alias `pusd.html`. Enough.
- Isolated `target-*`. Use `target/`.
- Mixing overnight “no x402 settle” (01:01 goal) with v1.2 testnet settle (Relic later). Testnet settle is in. Mainnet is not.

## Ship truth

- Bar: `docs/V1.md`
- What runs: `docs/STATUS.md`
- Board: `docs/COORDINATION.md` + `docs/TRACKS.md`
- Packed **1.1.0** rev `223a962`. Zip at 10:28 was fat (trailers). **mp4 is gitignored and not rust-embedded.** Next pack is thin unless Relic says ship trailers.

Economy shape: `docs/econ/TESTNET_SHAPE.md`. `TESTNET_HOUSE` / `TESTNET_LOOP` stay empty until the bag wallet signs.
