# vapurr v1

We are **pre-v1**. Everything in this tree is aimed at one ship: a Windows zip a stranger can unzip and browse with.

v1 is a **browser with vapurr chrome and Robinhood Chain in the product**. It is not Servo. It is not live card KYC. It is not a protocol demo that cannot open google.com.

North star after v1 is still Servo, native wallet authority, x402, zmail, zer0ID, Rain passthrough. Those crates stay in the workspace and keep compiling. They are not the v1 critical path.

## What v1 is

A person unzips `dist\vapurr-<ver>-windows-x64.zip` and double-clicks **Install vapurr.exe**:

1. A native Windows window that looks like `DESIGN.md` (cat, lime from `frontend/tokens.css`, Sora).
2. Real https browsing in the page webview. **WebView2 is allowed for v1.** Do not rip it out to "be pure."
3. Chrome that is not a website: rail, toolbar, tabs, home, settings persisted in `desk.json`.
4. **Shield** actually blocking ads/trackers before paint.
5. **Scan** live against Robinhood Chain (`vapurr-rhc`), including the **liquidity graph** (entire-chain pools as the market data source).
6. **Live Trenches** opens **https://fomo.family** in this window (rail, home tile, `vapurr://fomo` / `vapurr://family`). Ketflix stays a separate chrome surface (`vapurr://ketflix`).
7. **$VAPURR / $PUSD** desk talks to **PusdMarket on Robinhood Chain**. Burn V mint P, burn P mint V. **Lithe** (`vapurr://lithe`) is 9% on `$PUSD`. Deploy from the desk.
8. Bookmarks, history, cookies, Boost/blob cache, Maestro radio.
9. **vapurrbid** (`vapurr://vapurrbid`) â€” $PUSD pay-to-rank. Aliases `outbid` / `bid` / `board`.
10. **PNS** (`vapurr://pns`) â€” Purr Name Service, TLD `.hood`. On-chain registry. Aliases `hood` / `names`.
11. `.\pack.ps1` produces `dist\vapurr-<ver>-windows-x64.zip`. Double-click **Install vapurr.exe** â€” branded first-run, no admin. `cargo test --workspace` is green. The on-chain market is not a gate to browse.

## What v1 is not

These wait until after v1.

| After v1 | Why it waits |
|---|---|
| Servo as the page engine | Guest is Edge until libservo is pinned. Feature `servo` is a compile error on purpose. |
| FetcherEngine as the user's browser | Protocol/reader crate. The window still navigates Edge. |
| egui chrome (`vapurr-ui`) | Leftover. Not linked by the shell. |
| Live Rain / Avalanche Card | No PAN, no live issuer secrets in tree. Card pane is a drawn plate. |
| Live zer0ID issuer | No issuer in this tree. `vapurr://id` is a sheet; Start KYC opens thesecretlab.app/kyc. Does not fake Proven. Earn payout stays blocked without VerifiedAccount. |
| KetPay settlement on mainnet 4663 | v1.2 settles `$PUSD` on testnet 46630 only. |
| zmail send/receive | Glass inbox. Send seals, pins a CID, signs a 0.25Â¢ $PUSD/$VAPURR voucher (0 ETH). Body never on-chain. Postage settlement waits on the live market. |
| One site-process per eTLD+1 + freeze | Types in `vapurr-core`. Guest is one Edge profile. |

## Honest skins (v1 may ship these as chrome)

These URLs must exist and match brand. They must **not** fake a successful payment, KYC, or mail send.

| Surface | v1 bar |
|---|---|
| `vapurr://wallet` | Glass portfolio for this device on Robinhood Chain. Live Trenches is a separate tab. |
| `vapurr://pay` / `ketpay` (KetPay) | x402 / `$PUSD`. Signs `wallet-send` on **testnet 46630** only. Refuses mainnet. |
| `vapurr://card` | Drawn VAPURR card. No Rain. |
| `vapurr://zzzmail` (`zmail`, `mail`) | iMessage-style glass inbox. `.hood` names. Send pins a CID and a gasless 0.25Â¢ voucher. Does not claim on-chain settlement until the market is live. |
| `vapurr://swap` / `vapurr://bridge` | Simulate on this device, then sign and broadcast. Full route + `$VAPURR` refund. |
| `vapurr://id` | zer0ID sheet. Prove a person, handle not passport. Start KYC opens thesecretlab.app/kyc. Does not fake Proven. Ad block is `vapurr://shield`. |

Living snapshot of what already runs: `docs/STATUS.md`.

## Earn exception
Browse-earn **payout** requires live zer0ID KYC (https://www.thesecretlab.app/kyc) and per-machine `install_id`. That pulls zer0ID onto the earn critical path even while other after-v1 items stay parked. See `docs/zeroid/RHC.md`.

## Savings source progress - 2026-09-05

The v1.2 money groundwork now includes a locally tested SavingsRouter for shared post-runway liquid/CD surplus and CD terms fixed at deposit. This does not add a browser ship gate or live savings claim. Bonds/CD controls are visible; missing market wiring is reported when used and terms remain unquoted. See docs/econ/SPUSD.md for contingent coupon settlement and docs/STATUS.md for validation.

DeFi UI now guides users through visual economic routes and selectable Lithe / Oliver / House desks. Swap and bridge share the same finance chrome and a Back stack to DeFi. The native signing flow remains in place. Offline Edge checks cover desktop/narrow layouts, both themes, transaction review, and swap/bridge routing. Packed into the local 1.1.9 installer and ZIP; package remains unsigned.
