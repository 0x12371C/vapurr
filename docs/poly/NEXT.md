# NEXT - poly desk

- Optional: `/poly/api/status` that reports a real local poly_data path / last sync time (honest empty if missing). No invented counts.
- Optional rail pin only if Relic wants it next to Live Trenches; default discovery is Home + dApps + omnibox.
- Do not embed fake tape. Keep archive as live https guest (same model as fomo.family).
