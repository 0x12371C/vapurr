# Ketbook - $PUSD market notes

Product dollar mint/redeem lives in `contracts/PusdMarket.sol`.
Stability-pool math reference: `contracts/market-math-ref/`.

Ship names only: `$VAPURR`, `$PUSD`, `swapVToPusd`, `swapPusdToV`, `vapurrRate`, `poolDelta`.