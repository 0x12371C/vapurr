# What works today

The market math above is the contract. The desk deploys a **fresh** book and mint/redeem. Deploy mints 1,000,000 $VAPURR to this device. No USDG seed. Retired testnet CAs do not count.

The Euler-shaped vault (`PusdLoop`) is the next contract. Same desk. Empty CA until you deploy it.

The house Uniswap v4 `$VAPURR` / `$PUSD` book is live on testnet 46630 (gen-4). Retired CAs do not count.

You can browse, Shield, Scan, hold a wallet on this PC, open Live Trenches, use the $PUSD desk, bid the floor, claim a `.hood` name (testnet registry), get a swap quote, and keep mail on this device.

KetPay sends `$PUSD` on testnet 46630. Not live, and the screens do not fake it: KetPay on mainnet, card spend, KYC, zzzmail settling postage on chain, swap/bridge execution.
