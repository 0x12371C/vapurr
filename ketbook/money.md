# $PUSD

Burn `$VAPURR`, mint `$PUSD`. Burn `$PUSD`, mint `$VAPURR`. No USDG in the swap. Oracle first spot of the block. Min 2% stability spread. **Lithe** is 9% on `$PUSD`.

`$PUSD` is what the browser spends: KetPay, zzzmail postage, vapurrbid. Supply and borrow on `$PUSD` (Euler-shaped vault) is how that dollar gets depth — see [Supply and borrow](euler.md). See [The loop](loop.md).

`vapurr://pusd`
