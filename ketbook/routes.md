# Swap and bridge

Live quote. **25 bps** vapurr scoop (`ROUTE_FEE_BPS`), same shape as a router fee.

The button copies the route and opens Wallet. It does not swap. It does not bridge.

USDG may appear on the quote because that is the chain’s dollar. The mint is still burn `$VAPURR` → `$PUSD`.
